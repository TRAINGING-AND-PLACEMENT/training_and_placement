@model DepartmentSectorCsv

@{
    ViewData["Title"] = "Department CSV";
}


<div class="main-content">
    <div class="page-content">
        <div class="container-fluid mt-5">
            <div class="row">
                <div class="col-md-10">
                    <div class="card">
                        <div class="card-body">
                            <form asp-controller="Uploadcsv" asp-action="Department" method="post" enctype="multipart/form-data">
                                <div class="form-group">
                                    <label>Upload Department CSV</label>
                                    <input type="file" asp-for="@Model.CSV_File" name="CSV_File" class="form-control " />
                                    <span class="text-danger" asp-validation-for="@Model.CSV_File"></span>
                                </div>
                                <br>
                                <div class="form-group">
                                    <input type="submit" value="Import" class="btn btn-primary" />
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
</div>
    @section Scripts{
        @{
            <partial name="_ValidationScriptsPartial" />
        }
    }
